{
    "setup_steps" : {
                        "CLEANUP"       : "PYTHON ./cleanup_installation.py",
                        "GENPACKAGEDOC" : "PYTHON ./genpackagedoc.py",
                        "INSTALL"       : "PYTHON -m pip install -v --proxy PROXYSETTINGS .",
                        "BUILD"         : "PYTHON -m build ."
                    },
    "setup_type" :  {
                        "install" : ["CLEANUP", "GENPACKAGEDOC", "INSTALL"],
                        "build"   : ["GENPACKAGEDOC", "BUILD"]
                    }
}
